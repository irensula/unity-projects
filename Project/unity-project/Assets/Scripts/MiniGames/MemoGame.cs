using UnityEngine;

public class MemoGame : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Mini game 2 is opened");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
