using UnityEngine;

public class Game3 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Mini game 3 is opened");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
