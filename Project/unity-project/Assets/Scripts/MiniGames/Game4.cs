using UnityEngine;

public class Game4 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Mini game 4 is opened");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
